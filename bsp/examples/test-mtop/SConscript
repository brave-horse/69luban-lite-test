Import('AIC_ROOT')
Import('PRJ_KERNEL')
from building import *

cwd = GetCurrentDir()
CPPPATH = []
src = []
if GetDepend('AIC_MTOP_DRV') and GetDepend('RT_USING_FINSH'):
    src = Glob('*.c')

group = DefineGroup('test-mtop', src, depend = [''], CPPPATH = CPPPATH)

Return('group')
