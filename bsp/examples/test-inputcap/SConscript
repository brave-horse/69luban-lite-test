Import('AIC_ROOT')
Import('PRJ_KERNEL')
from building import *

cwd = GetCurrentDir()
CPPPATH = []
src = []
if GetDepend('AIC_INPUTCAP_DRV_TEST'):
    src = Glob('*.c')

group = DefineGroup('test-inputcap', src, depend = [''], CPPPATH = CPPPATH)

Return('group')
